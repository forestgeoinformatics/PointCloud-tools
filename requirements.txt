laspy[lazrs]>=2.0
numpy>=1.24
scipy>=1.10
tqdm>=4.65
pandas>=2.0
scikit-learn>=1.3
open3d>=0.17          # required only by restore_density.py (optional)
jakteristics>=0.5     # required only by compute_metrics_jakteristics.py (optional)
